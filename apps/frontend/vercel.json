{
  "build": {
    "command": "pnpm install && nx build frontend",
    "outputDirectory": "apps/frontend/dist"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
