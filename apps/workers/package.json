{
  "name": "@prodgenie/workers",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@prodgenie/workers:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@prodgenie/workers:build:development"
          },
          "production": {
            "buildTarget": "@prodgenie/workers:build:production"
          }
        }
      }
    }
  }
}
